box: ubuntu
build:
    steps:
        - rdcojocaru/create-job@1.0.0:
            json_file: job.json
            dcos_url: http://localhost
            time_limit: 100
        - script:
            name: prepare output
            code: rm -rf $WERCKER_ROOT/.git
