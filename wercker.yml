box: alpine
build:
    steps:
        - rdcojocaru/trigger-test@1.0.0:
            message: 'TEST MESSAGE!'
        - script:
            name: prepare output
            code: rm -rf $WERCKER_ROOT/.git
